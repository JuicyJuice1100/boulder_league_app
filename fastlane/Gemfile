source "https://rubygems.org"

gem "fastlane"

# Ruby 3.4+ compatibility
gem "abbrev"
gem "base64"
gem "bigdecimal"
gem "mutex_m"
gem "ostruct"

# Load Fastlane plugins
plugins_path = File.join(File.dirname(__FILE__), '.', 'Pluginfile')
eval_gemfile(plugins_path) if File.exist?(plugins_path)
